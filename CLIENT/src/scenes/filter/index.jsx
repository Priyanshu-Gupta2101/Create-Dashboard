import Header from "../../components/Header";
import { Box } from "@mui/material";
const Filter = () => {
  return (
    <Box marginLeft={4} marginTop={4}>
      <Header title="Filters" subtitle="Visualize the filters here" />
    </Box>
  );
};

export default Filter;
